1. make discounttype select and option
2.make the date readable and show created at 
 for this date utils-> file is made and formatted in table-> to show created at date
  to use :
  formatdate(item.createdat,'ddd YYYY/MM/DD)
  formatdate is a function called from utility component folder 
  to get time only
  formatdate(item.createdat,'hh:mm a')

3. place the value of the manu date by formatting using moment js utils library as you did for discountedItem the same process
 if there is manu date than format the date in YYYY-MM-DD but in the form it will  be shown as YYYY/MM/DD

 productform line no.54 inside setstate

 file upload 
 1. make input for file type=file
 2. handlechange
 if(type=='file){
     do something
     clg(file)
 }
 this file will be taken to server with the help of xml http request

 before pushing the files need to be saved in a state 
 make a state nanmed
  filesToupload:[]
  and it immediately needs to be shown so make 
  filestoShow:[]

  in onchange function
  const {filesToupload,filestoShow}=this.state
  filesToupload.push(files[0])
  filestoShow.push(URL.createObjectURL(files[0]))
  this.setState({
      filesToupload,
      filestoShow
  })

  and show it on the uI as
   <input type="file" className="form-control" onChange={this.handleChange}></input>
                    {
                        this.state.filetopreview.map((file, index) => {
                            return (
                                <div style={{ marginTop: '10px' }} key={index} >
                                    <img src={file} alt="preview.png" width="200px"></img>
                                    <span onClick={() => this.removeImage(index)} title="Remove Image" style={{ marginLeft: '5px', color: 'red' }}>
                                        <FaTrashAlt></FaTrashAlt>
                                    </span>
                                </div>

                            )
                        })
                    }


make a removeImage function
    removeImage(index){
        const { filetoupload, filetopreview } = this.state
        filetoupload.splice(index,1)
        filetopreview.splice(index,1)
        this.setState({
            filetoupload,
            filetopreview
        })
    }


successfully removed on UI now insert into database using XML HTTP request
it requires form data so
the call is done in Httpclient file

handlesubmit will send (data,filetosend) to a callback function -> of productform

xhr will send the post request from form data method

now show the image uploaded in view page a single image

you have a /file middleware in express server and you serve from that file
make a env file for storing the image link

REACT_APP_IMG_URL="http://localhost:4000/file/images"

now using this link in img src section you can show the images as

  <tr key={index}>
                                            <th scope="row">{index + 1}</th>
                                            <td><Link to={`/productdetails/${item._id}`}>{item.name}</Link></td>
                                            <td>{item.category}</td>
                                            <td>{formatDate(item.createdAt,"YYYY:MM:DD hh:mm a")}</td>
                                            <td>{item.price}</td>
                                            <td> <img src={`${IMG_URL}/${item.images[0]}`} alt="product.png" width="200px"></img> </td>
                                            <td><button onClick={() => this.editproduct(item._id)} title="edit product"><FaPencilAlt /></button> <button onClick={() => this.removeproduct(item._id, index)} title="delete product"><FaTrash /></button></td>
                                        </tr>

which is done in img section
